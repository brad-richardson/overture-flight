{
  "name": "flight-sim",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm test && tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "party:dev": "partykit dev",
    "party:deploy": "partykit deploy",
    "generate:trees:sample": "node scripts/generate-sample-tree-data.js",
    "generate:trees": "uv run scripts/generate-tree-tiles.py"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^2.0.4",
    "earcut": "^3.0.2",
    "maplibre-gl": "^5.0.0",
    "partysocket": "^1.0.2",
    "pbf": "^4.0.1",
    "pmtiles": "^4.3.2",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "@types/earcut": "^3.0.0",
    "@types/pbf": "^3.0.5",
    "@types/three": "^0.182.0",
    "partykit": "^0.0.111",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.2.7"
  }
}
